import 'package:flutter/material.dart';

class HomeBannerModel {
  String? icon;
  String? title;
  String? subtitle;
  Color? color;
  Color? iconColor;
  Color? textColor;
  HomeBannerModel(this.icon, this.title, this.subtitle, this.color,
      this.iconColor, this.textColor);
}
